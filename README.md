# EWM - Asystent Pracy Wychowawczej

### Instalacja

By zainstalować projekt należy po skopiowaniu plików wykonać w folderze głównym, następujące komendy:

```sh
$ bundle install
$ rake db:migrate
$ rake db:seed
```
